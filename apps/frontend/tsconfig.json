// apps/frontend/tsconfig.json (최종 완성본)
{
  // 1. [핵심] 프로젝트 루트의 "기반 헌법"을 상속받습니다.
  //    이제 이 파일은 tsconfig.base.json의 모든 규칙(paths 포함)을 알게 됩니다.
  "extends": "../../tsconfig.base.json", 
  "compilerOptions": {
    // 2. Next.js가 필요로 하는 플러그인을 추가합니다.
    "plugins": [{ "name": "next" }],
    
    // 3. [핵심] 상속받은 paths 설정을, 현재 프로젝트("수도")에 맞게 재정의(override)합니다.
    //    이렇게 하면 VSCode와 Next.js가 경로를 해석할 때 혼란을 겪지 않습니다.
    "baseUrl": ".", // 기준 경로는 현재 파일이 있는 위치입니다.
    "paths": {
      "@/*": ["./src/*"] 
    },

    // 4. Next.js에 필요한 나머지 설정들을 명시합니다.
    "allowJs": true,
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "incremental": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "lib": ["dom", "dom.iterable", "esnext"]
  },
  // 5. 이 법률이 적용될 파일의 범위를 지정합니다.
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}