'use client';

import { useState } from 'react';
import SignUp from '@/components/SignUp';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export default function Home() {
  const [mousePos, setMousePos] = useState({ x: 0, y: 0 });

  const handleMouseMove = (e: React.MouseEvent<HTMLDivElement, MouseEvent>) => {
    const x = (e.clientX / window.innerWidth - 0.5) * 10; // -5~5px 정도로 subtle
    const y = (e.clientY / window.innerHeight - 0.5) * 10;
    setMousePos({ x, y });
  };

  return (
    <main
      className={`relative flex min-h-screen flex-col items-center justify-between bg-gradient-to-br from-sky-100 via-sky-200 to-blue-400 p-4 overflow-hidden ${inter.className}`}
      onMouseMove={handleMouseMove}
    >
      {/* 배경 */}
      <div
        className="absolute top-0 left-0 w-full h-full"
        style={{
          transform: `translate(${mousePos.x}px, ${mousePos.y}px)`,
          transition: 'transform 0.2s ease-out',
          zIndex: 0,
        }}
      />

      {/* 카드 박스 */}
      <div className="relative z-10 w-full max-w-lg mx-auto mt-12 rounded-xl bg-white/80 backdrop-blur-sm p-10 shadow-2xl transition-all duration-500 hover:bg-white/90 hover:shadow-3xl">
        <div className="mb-10 text-center">
          <h1 className="text-5xl font-extrabold text-sky-600 drop-shadow-lg transition-colors duration-300 hover:text-blue-500">
            Deep Dive!🐬
          </h1>
          <p className="mt-6 text-lg font-medium text-blue-800 transition-transform duration-300 hover:rotate-1">
            🛠️인테리어 & 여름  {'천국\'s'} 휴가 중⛵
          </p>
        </div>

        {/* SignUp wrapper: hover/scale 효과 */}
        <div className="transition-transform duration-300 hover:scale-102 hover:shadow-md">
          <SignUp />
        </div>
      </div>

      {/* 최하단 푸터 */}
      <footer className="w-full text-center text-sm text-gray-500 mt-8">
        © {new Date().getFullYear()} My Project
      </footer>
    </main>
  );
}
