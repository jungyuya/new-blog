// 파일 위치: apps/frontend/src/app/layout.tsx (수정)
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import MainLayout from "@/components/MainLayout"; // [신규] MainLayout 클라이언트 컴포넌트 import

const inter = Inter({ subsets: ["latin"] });

// [핵심] 이제 이 파일은 서버 컴포넌트이므로, metadata를 다시 export 할 수 있습니다.
export const metadata: Metadata = {
  title: "Deep Dive!",
  description: "Generated by LEEJUNGYU",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
 return (
    <html lang="ko" suppressHydrationWarning> 
      <body className={inter.className}>
        {/* [핵심] 모든 클라이언트 사이드 로직을 MainLayout 컴포넌트에 위임합니다. */}
        <MainLayout>
          {children}
        </MainLayout>
      </body>
    </html>
  );
}