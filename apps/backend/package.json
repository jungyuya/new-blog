{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend Lambda function for the blog service",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "zip": "cd dist && zip -r ../backend.zip .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "pnpm build && sam local start-api --env-vars ./env.json --docker-volume-basedir . --template template.yaml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.1.0",
    "@types/uuid": "^10.0.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.848.0",
    "@aws-sdk/client-dynamodb": "^3.848.0",
    "@aws-sdk/lib-dynamodb": "^3.850.0",
    "@hono/zod-validator": "^0.7.2",
    "hono": "^4.8.10",
    "uuid": "^11.1.0",
    "zod": "^4.0.14"
  }
}