// ~/projects/new-blog/tsconfig.json
{
  // 이 파일은 실제 컴파일을 수행하지 않으며,
  // 오직 VSCode와 같은 도구에게 프로젝트들의 관계를 알려주는 역할만 합니다.
  "files": [],
  "include": [],
  "references": [
    { "path": "apps/backend" },
    { "path": "apps/frontend" },
    { "path": "apps/infra" }
  ]
}