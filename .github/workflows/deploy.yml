# .github/workflows/deploy.yml (중복 제거 및 순서 확정 최종안)

name: "Deploy Full-Stack Application to AWS"

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: "Phase 1: Setup Environment"
        uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - uses: pnpm/action-setup@v4
        with: { run_install: false }
      - uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-
      - name: Install dependencies at root
        run: pnpm install --frozen-lockfile

      # ===================================================================================
      # Phase 2: Build Prerequisite Assets
      # ===================================================================================

      - name: "Step 2.1: Clean previous artifacts"
        run: pnpm run clean

      - name: "Step 2.2: Build Backend"
        working-directory: ./apps/backend
        run: pnpm run build
      
      - name: "Step 2.3: Build Frontend Static Assets"
        working-directory: ./apps/frontend
        run: pnpm run build  

      # ===================================================================================
      # Phase 3: Deploy to AWS
      # ===================================================================================

      - name: "Step 3.1: Configure AWS Credentials"
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Set CDK Qualifier
        run: echo "CDK_QUALIFIER=hnb659fds" >> $GITHUB_ENV

      - name: "Step 3.2: Bootstrap CDK with explicit Qualifier"
        run: pnpm --filter infra exec cdk bootstrap --qualifier ${{ env.CDK_QUALIFIER }}

      - name: "Step 3.3: Deploy Infrastructure and Application via CDK"
        # [핵심] deploy 명령어에도 동일한 Qualifier를 전달하여,
        # bootstrap과 deploy가 100% 동일한 저장소 이름을 사용하도록 보장합니다.
        run: pnpm --filter infra exec cdk deploy --all --require-approval never --qualifier ${{ env.CDK_QUALIFIER }}