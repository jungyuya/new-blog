{
  "name": "image-processor-service",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --external:sharp --external:@aws-sdk/* --outfile=dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.873.0",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/sharp": "^0.31.1",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2"
  }
}